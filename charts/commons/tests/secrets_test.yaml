suite: Tests complets du chart commons
release:
  name: test
tests:
  # --- Secrets ---
  - it: Vérifie le Secret Redis
    templates:
      - templates/redis/secret.yaml
    documentSelector:
      path: metadata.name
      value: test-redis-secret
    asserts:
      - equal:
          path: type
          value: Opaque
      - equal:
          path: data.password
          value: Y2hhbmdlbWU=

  # --- Secret ---
  - it: Vérifie le Secret Postgres
    templates:
      - templates/cnpg/secret.yaml
    documentSelector:
      path: metadata.name
      value: test-postgres-secret
    asserts:
      - equal:
          path: type
          value: kubernetes.io/basic-auth
      - equal:
          path: data.username
          value: Y2hhbmdlbWU=
      - equal:
          path: data.password
          value: Y2hhbmdlbWU=