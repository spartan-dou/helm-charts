# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: Tests complets du chart commons
values:
  - ./values/values.yaml
templates:
  - templates/configmap.yaml
release:
  name: test
tests:
  # --- Configmap ---
  - it: Vérifie le configmap
    documentSelector:
      path: metadata.name
      value: test-nginx-config
    asserts:
      - equal:
          path: data["config.yaml"]
          value: |
            key: value
      - equal:
          path: data["toto.sh"]
          value: |
            key: value